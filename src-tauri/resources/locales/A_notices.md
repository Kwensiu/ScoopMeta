# Note: Files in this directory are automatically copied from src/locales during build.

Do not edit files in this directory directly. All localization files should be edited in the `src/locales` directory.

During the build process, these files are automatically copied here for Tauri backend usage.

# 注意：此目录中的文件会在构建时自动从 src/locales 复制

请勿直接编辑此目录下的文件。所有本地化文件应在 `src/locales` 目录下进行编辑。

构建过程中，这些文件会被自动复制到此处以供 Tauri 后端使用。
